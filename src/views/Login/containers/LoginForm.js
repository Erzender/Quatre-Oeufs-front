import { connect } from 'react-redux';

import LoginFormCpt from '../components/LoginForm';

const mapStateToProps = () => {};

const mapDispatchToProps = () => {};

export default connect(
  mapStateToProps,
  mapDispatchToProps,
)(LoginFormCpt);
